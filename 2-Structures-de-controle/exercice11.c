#include <stdio.h>
//11. Vérifier si un nombre est positif, négatif ou nul.
int main() 
    {int number = -51837;
    if (number < 0) {printf("Negatif.");}
    else if (number == 0) {printf("Nul.");}
    else {printf("Positif.");}
    return 0;}